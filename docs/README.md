# Docs

This directory is intentionally small.

- [architecture.md](architecture.md): runtime flow, storage layers, query path, and native boundary
- [design.md](design.md): design choices, layout rationale, encodings, pruning model, and non-goals

The older milestone-by-milestone notes were removed so the docs reflect the current system rather than the implementation history.
